# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
prod.tfvars

# Sensitive variable files
*.auto.tfvars
*.auto.tfvars.json
# Note: dev.tfvars is committed (no secrets in it)
# prod.tfvars should NOT be committed if it contains secrets

# OS
.DS_Store
